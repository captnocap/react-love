=== METADATA ===
title: BarChart
description: Vertical bar chart that renders data as proportionally sized colored bars with optional labels and values.
category: Components
platforms: love2d, web, terminal, cc, nvim, hs, awesome
keywords: bar chart, chart, graph, visualization, data, bars
related: Sparkline, ProgressBar, Table
difficulty: beginner

=== OVERVIEW ===
BarChart renders a vertical bar chart from an array of data points. Each bar's height is proportional to its value relative to the maximum value in the dataset. Bars can have individual colors or share a default color.

The component renders entirely with Box and Text primitives, so it works on all targets. An optional label row appears below the bars, and values can be shown above each bar.

=== API / SYNTAX ===
Import: import { BarChart } from '@ilovereact/core';

BarChartBar type:
```ts
interface BarChartBar {
  label: string;
  value: number;
  color?: Color;
}
```

Usage:
```tsx
<BarChart
  data={[
    { label: 'Mon', value: 12 },
    { label: 'Tue', value: 28 },
    { label: 'Wed', value: 19 },
    { label: 'Thu', value: 35 },
    { label: 'Fri', value: 24 },
  ]}
  height={100}
/>
```

Props:

| Prop       | Type            | Default    | Description                                |
|------------|-----------------|------------|--------------------------------------------|
| data       | BarChartBar[]   | (required) | Array of bar data points                   |
| height     | number          | 120        | Chart area height in pixels                |
| barWidth   | number          | 20         | Width of each bar in pixels                |
| gap        | number          | 8          | Gap between bars in pixels                 |
| showLabels | boolean         | true       | Show label text below each bar             |
| showValues | boolean         | false      | Show value text above each bar             |
| color      | Color           | '#3b82f6'  | Default bar color (overridden per-bar)     |
| style      | Style           | -          | Outer container style overrides            |

=== EXAMPLES ===
Example 1: Basic bar chart
---
```tsx
<BarChart
  data={[
    { label: 'Q1', value: 40 },
    { label: 'Q2', value: 65 },
    { label: 'Q3', value: 52 },
    { label: 'Q4', value: 78 },
  ]}
/>
```
---
Platforms: All

Example 2: Custom colors per bar with values
---
```tsx
<BarChart
  data={[
    { label: 'CPU', value: 72, color: '#ef4444' },
    { label: 'RAM', value: 45, color: '#3b82f6' },
    { label: 'Disk', value: 89, color: '#f59e0b' },
  ]}
  height={80}
  barWidth={24}
  showValues
/>
```
---
Platforms: All

Example 3: Wide bars with no labels
---
```tsx
<BarChart
  data={dataPoints}
  barWidth={32}
  gap={4}
  showLabels={false}
  color="#22c55e"
  height={60}
/>
```
---
Platforms: All

=== PLATFORM NOTES ===
- BarChart uses Box and Text primitives exclusively. No canvas, SVG, or target-specific drawing APIs.
- The chart width is computed automatically: `data.length * barWidth + (data.length - 1) * gap`.
- Each bar height is calculated as `Math.max(1, Math.round((bar.value / maxValue) * height))`, ensuring every bar is at least 1px tall.
- Label text is `fontSize: 9`, `color: '#64748b'`. Value text is `fontSize: 9`, `color: '#94a3b8'`.
- Bars use `borderRadius: 3` for slightly rounded tops.
- When `data` is empty, an empty Box with the specified `height` is rendered.

=== CRITICAL RULES ===
- The `data` prop is required. An empty array renders an empty placeholder.
- Bar heights are relative to the maximum value in the data. If all values are equal, all bars render at full height.
- If `maxValue` is 0 (all values are 0), bars are normalized against 1, so they appear as minimum height (1px).
- Per-bar `color` overrides the component-level `color` prop for that specific bar.
- BarChart does not handle negative values. All values should be non-negative.

=== SEE ALSO ===
- Sparkline
- ProgressBar
- Table
